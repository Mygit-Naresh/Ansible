- name: nginx install and Run
  hosts:  web
  become: true
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Run nginx
    ansible.builtin.service:
      name:  nginx
      state: started
   